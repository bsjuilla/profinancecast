{
  "name": "profinancecast",
  "version": "1.0.0",
  "private": true,
  "description": "Static HTML deploy on Vercel with serverless functions in /api. This package.json exists only so Vercel installs the dependencies the api/ functions need; there is no build step.",
  "dependencies": {
    "@vercel/og": "^0.6.4"
  }
}
