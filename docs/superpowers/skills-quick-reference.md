# Skills Quick Reference

A 60-second lookup of which Skill to invoke for common ProFinanceCast work. Invoke via the Skill tool (`skill: "<name>"`), not the Agent tool.

## Engineering & debugging

| Skill | When to use |
|---|---|
| `systematic-debugging` | Stuck on a bug; need a disciplined repro → isolate → fix loop instead of guessing. |
| `debugging-wizard` | Heavy, multi-layer bug (frontend + backend + data). Heavier than systematic-debugging. |
| `test-driven-development` | Writing a new utility, hook, or pure function. Forces failing test first. |
| `code-review-excellence` / `pr-review-expert` | Reviewing a diff before merge. Catches reuse, perf, security issues. |
| `verification-before-completion` | Before you say "done." Runs the change against acceptance criteria. |
| `pair-programming` | Working through a hard refactor live; want a second voice. |

## Design & frontend polish

| Skill | When to use |
|---|---|
| `frontend-design` | Build or fix a React/HTML component to feel premium. Default for UI work. |
| `ui-ux-pro-max` | Higher-fidelity UX pass — layout, density, hierarchy on a key screen. |
| `taste-skill` | Quick "does this look good?" gut check; refines visuals without rebuilding. |
| `redesign-skill` | Existing screen looks dated; want a clean redesign pass. |
| `critique` | 5-dimension scored design review of an HTML artifact. Use after a draft. |
| `extract-design` | Pull tokens (color, type, spacing) out of a reference site or screenshot. |

## Product & planning

| Skill | When to use |
|---|---|
| `pm-spec` | Turning a fuzzy idea into a written spec with scope and acceptance criteria. |
| `brainstorming` | Need 10 angles before picking one. Diverges, doesn't converge. |
| `writing-plans` | Multi-step work that needs a written plan before coding. |
| `doc-coauthoring` | Long-form internal docs (proposals, decision records). |

## Marketing, CRO, content (ProFinanceCast growth)

| Skill | When to use |
|---|---|
| `copywriting` | Headlines, CTAs, email body. Default for any user-facing words. |
| `copy-editing` | Polishing existing copy — tighter, clearer, on-brand. |
| `brand-guidelines` | Applying Anthropic/site brand colors + type to an artifact. |
| `page-cro` | Conversion review of a landing or pricing page. |
| `paywall-upgrade-cro` | Upgrade flow / paywall conversion specifically. |
| `seo-audit` | Audit a page for on-page SEO issues. |
| `schema-markup` | Add JSON-LD structured data to a page. |
| `pricing-page` / `pricing-strategy` | Designing or revising the pricing page or tiers. |

## Meta / workflow

| Skill | When to use |
|---|---|
| `using-superpowers` | Start of a multi-step session — loads the orchestration playbook. |
| `verify` | Manually verify a change by actually running the app. |
| `run` | Just launch ProFinanceCast locally and screenshot. |
| `code-review` | Targeted review-and-fix pass on changed files. |
| `security-review` | Security pass over pending changes on the current branch. |
| `graphify` | Build a knowledge graph of the codebase or a doc set. |
